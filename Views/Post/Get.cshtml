@model Forum.Models.Post

@{
    ViewData["Title"] = "Post";
}


<div class="above-threads mb-2">
  <a type="button" class="btn btn-primary" href="/posts/@Html.DisplayFor(modelItem => modelItem.ID)/reply">Post a Reply</a>
</div>

<div class="container">
    <div class="row my-3">
        <div class="col-4 border">
            <div>
                @Html.DisplayFor(modelItem => modelItem.Author.UserName)
            </div>
            <div>
                @Html.DisplayFor(modelItem => modelItem.Author.JoinDate)
            </div>
        </div>
        <div class="col-6 border">
            @Html.DisplayFor(modelItem => modelItem.Body)
        </div>
    </div>
    @* @foreach (Post post in Model.Posts) *@
    @* { *@
    @*     <div class="row my-2"> *@
    @*         <div class="col-2 border"> *@
    @*             <div> *@
    @*                 @Html.DisplayFor(modelItem => post.Author.UserName) *@
    @*             </div> *@
    @*             <div> *@
    @*                 @Html.DisplayFor(modelItem => post.Author.JoinDate) *@
    @*             </div> *@
    @*         </div> *@
    @*         <div class="col-10 border"> *@
    @*             @Html.DisplayFor(modelItem => post.Body) *@
    @*         </div> *@
    @*     </div> *@
    @* } *@
</div>
